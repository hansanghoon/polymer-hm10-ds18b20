{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "users" : {
      "$uid" : {
        "devices" : {
          "$device" : {
            "history" : {
              // ".indexOn" : "timestamp"
            }
          }
        }
      }
    }
  }
}